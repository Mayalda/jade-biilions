name: Billions Agent Verify

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run verification
        run: echo "Agent verification running"
